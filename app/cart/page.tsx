import OrderSection from "@/sections/order_section/OrderSection";

const Cart = () => {
  const links = [{ title: "Cart", href: "" }];

  return (
    <div>
      <OrderSection />
    </div>
  );
};

export default Cart;
